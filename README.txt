Jyotika Surprise Website (multi-page)
Open: index.html

Preview:
- Click "Preview all days" (top-right) to test before the date.

Songs (easy to change):
- Edit songs.js (YouTube IDs + titles).
- Dates/order are in config.js (SURPRISE_DATES).

YouTube player:
- Every page shows the player box.
- Use "▶️ Play inside page" to load the embed (best compatibility).
